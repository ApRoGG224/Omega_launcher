{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "debug_window", "ms_auth"],
  "permissions": [
    "core:default",
    "core:window:allow-create",
    "opener:default"
  ]
}
